version: '3.8'
services:
  client-backend:
    image: client:0.0.1
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - service-db

  service-db:
    image: redis
    ports:
      - "6379:6379"
    restart: unless-stopped